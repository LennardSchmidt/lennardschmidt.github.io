# lennardschmidt.github.io

This repository serves exclusively as a GitHub Pages redirect to my custom domain, which is hosted on Cloudflare. 

It contains an SEO-optimized `index.html` file that redirects visitors to [lennardschmidt.xyz](https://lennardschmidt.xyz) while passing along SEO ranking via a canonical tag.
